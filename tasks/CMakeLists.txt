# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(visualizer_moduleTaskLib)
ADD_LIBRARY(${VISUALIZER_MODULE_TASKLIB_NAME} SHARED 
    ${VISUALIZER_MODULE_TASKLIB_SOURCES})

add_dependencies(${VISUALIZER_MODULE_TASKLIB_NAME}
    regen-typekit)


TARGET_LINK_LIBRARIES(${VISUALIZER_MODULE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${VISUALIZER_MODULE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${VISUALIZER_MODULE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${VISUALIZER_MODULE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${VISUALIZER_MODULE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${VISUALIZER_MODULE_TASKLIB_HEADERS}
    DESTINATION include/orocos/visualizer_module)

